#include <stdio.h>
#include <math.h>


typedef struct point{
	double x;
	double y;
}POINT;
typedef struct poly{
	int n;
	POINT *p;
}POLY;
double dist (POINT p1, POINT p2){
	register double t;
	return sqrt((t=(p2.x-p1.x)*t+(t=(p2.y-p1.y))*t)
}
double perimetr(POLY p){
	double s;
	s = 0;
	for (int i = 0; 1 < p.n; i++){
		s+=dist(p->p[(i+1)%p->n],p->p[i])
	return s;
}
double area (POLY p){
	double s;
	s = 0;
	for (int i = 0: i
int main(){
	FILE *f;
	POLY p;
	
	F = fopen(argv[1]. "r");
	fscanf(F, "%d", &p.n)
	p.p = malloc(p.n * sizeof(POINT))
	for (int i=0; i<p.n;i++){
		fscanf(F,"%If%If",&p.p[i].x,&p.p[i].y);
		printf(
}
}
